Has subido la portada del libro en la conversación.

Para que la página muestre la imagen localmente, coloca el fichero de imagen con este nombre:

  images/cover.jpg

Recomendado:
- Formato: .jpg o .png
- Tamaño aproximado: 1200x1800 px (o menor)

Opciones para que lo haga yo por ti:
1) Si quieres que guarde la imagen desde el chat en el repositorio, confirma y yo intentaré escribir el binario en `images/cover.jpg`.
2) Sube aquí el archivo directamente (adjunta el fichero) y yo lo escribiré en la ruta anterior.

Si ya has colocado el archivo, abre http://127.0.0.1:5000 para comprobarlo.

